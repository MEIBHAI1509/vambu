import { WorkspacePlaceholder } from '@/components/workspace-placeholder'

export default function SettingsPage() {
  return (
    <WorkspacePlaceholder
      title="Settings"
      description="Notifications, privacy, and account preferences. Extend this page with forms backed by your profile API."
    />
  )
}
