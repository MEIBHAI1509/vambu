import { WorkspacePlaceholder } from '@/components/workspace-placeholder'

export default function FilesPage() {
  return (
    <WorkspacePlaceholder
      title="Files"
      description="Shared files and uploads from chats. Connect to Supabase Storage listings or another file provider."
    />
  )
}
